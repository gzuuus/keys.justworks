# dependencies
node_modules/

# rust
target/

# sqlite (dev db from `cargo run`)
*.db
*.db-wal
*.db-shm

# typescript build output
dist/

# web build output — keep the committed placeholder the server embeds at compile time
packages/web/build/*
!packages/web/build/index.html

# vendored reference repos (third-party, read-only)
references/

# env / misc
*.log
.DS_Store
.env
.env.*
