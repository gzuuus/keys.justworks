# keep the docker context small and free of local state
.git
.github
target/
**/node_modules/
**/.svelte-kit/
references/

# local dev db + web build (the image builds the site fresh in stage 1)
*.db
*.db-*
packages/web/build/

# env / editor / os
.env
.env.*
*.log
.DS_Store
Dockerfile
.dockerignore
